import React from 'react';

const Header = () => {
    return (
        <header>
            <div className="hero">
                <h1>FIZZA ALI</h1>
                <h2>Portfolio</h2>
            </div>
        </header>
    );
};

export default Header;
